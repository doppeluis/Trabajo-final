<!--Esta es la vista que se ve despues de ingresar a la aplicacion en la seccion perfil  -->
<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    <a href="/links" class="btn btn-primary m-4">
                        Guarda tu inventario   
                    </a>
                </div>
            </div>
        </div>

    </div>
</div>